<!DOCTYPE html>
<html>
  <head>
    <title>Search Page</title>
    <link rel="stylesheet" href="css\style.css">
  </head>
  <body>
    <div style="text-align:center;">
      <h1>Search Pass Number</h1>
      <form action="/" method="post">
        <input type="text" name="search" placeholder="Code Number...">
        <br>
        <% if(status=="Invalid") { %>
            <span style="color: red;">Invalid</span>
        <%}else if(status=="Sucess") { %>
            <span style="color: rgb(11, 235, 29);">You have successfully availed your offer!!</span>
        <% }else if(status=="used") { %>
            <span style="color: red;">Already used</span>
        <% }else{ %>
            <span></span>
        <% } %>
        <br>
        <button type="submit">Search</button>
      </form>
    </div>
  </body>
</html>
